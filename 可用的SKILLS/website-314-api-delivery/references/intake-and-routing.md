# Intake And Routing

## Intake Fields

For every new target site, capture:

- target URL
- business goal
- country/market/language
- login requirement
- transaction or payment risk
- expected API endpoints
- known errors or WAF markers
- proxy/IP requirement
- output service shape

## Task Classification

| Class | Meaning | Primary Skill |
|---|---|---|
| pure-http | Plain API with stable params | `reverse-js-crawler` |
| js-crypto | sign/token/cookie generated by JS | `reverse-js-crawler` |
| env-patch | official JS needs browser globals in Node | `reverse-js-crawler` |
| waf | Incapsula/Reese84/WAF/fingerprint challenge | `imperva-waf-reese84` |
| adapter | existing findings need standard API packaging | `site-api-adapter` |
| 314-delivery | user asks for service/API using 314 | `website-314-api-delivery` |
| governance | scoring, evals, drift, versioning | `skills-evaluation-governance` |

## Flight Booking Stages

For airline or booking websites, classify stages separately:

- search / air bounds
- fare details
- cart / offer selection
- traveler profile
- order detail
- payment preparation
- payment submit
- ticketing / confirmation

Each stage can have a different auth, WAF, host, cookie domain, payload, and success criterion.

## Routing Rules

- Check official frontend route and configuration before calling business APIs.
- Do not force a route into a new API flow if the official page routes it to legacy.
- Treat WAF HTML as protection failure, not empty business data.
- Treat payment submit as irreversible unless sandbox/dry-run is proven.

