name: Negative trigger for normal Markdown editing
prompt: >
  Rewrite this Markdown note to make the headings cleaner. There is no website,
  API, JavaScript, sign, token, crawler, or reverse engineering work.
criteria:
  - "Does not apply the crawler reverse workflow."
  - "Keeps the answer focused on Markdown editing."
expect_skill: false
timeout: 120

