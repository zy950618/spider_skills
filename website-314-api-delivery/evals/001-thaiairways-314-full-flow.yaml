name: Thai Airways full flow with 314
prompt: >
  I need pure API implementation for https://www.thaiairways.com/: search,
  cart, order creation, payment preparation, all crypto/anti-bot handling, and
  final APIs exposed through the 314 base framework.
criteria:
  - "Uses website-314-api-delivery as the overall orchestration skill."
  - "Separates search, cart, order, and payment stages with independent success criteria."
  - "Routes JS crypto to reverse-js-crawler and WAF/Reese84 to imperva-waf-reese84."
  - "Uses 314 integration because the prompt explicitly requests the 314 base framework."
  - "Requires dry-run/sandbox or explicit approval before real payment submission."
  - "Requires service-level and HTTP API tests plus stability loops."
expect_skill: true
timeout: 180

